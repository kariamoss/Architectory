# Lancer electron + ionic
```bash
cd client
npm run dev
```

# Lancer sur un appareil Android
```bash
cd client
npm run android
```

# Générer l'application Electron
```bash
cd client
npm run ebuild
```

Disponible par la suite dans le répertoire dist
